# Railway service config for the admin (god-mode) frontend (React Router + Nginx)

[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/admin/Dockerfile.admin"
buildContext = "."

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
